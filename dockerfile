# Используем официальный образ Node.js (например, Node 18)
FROM node:18

# Задаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы package.json и package-lock.json (если есть)
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем все остальные файлы
COPY . .

# Если у приложения есть порт для веб-интерфейса, можно указать его (если бот не слушает HTTP, этот шаг можно пропустить)
EXPOSE 3000

# Запускаем приложение
CMD ["node", "index.js"]
